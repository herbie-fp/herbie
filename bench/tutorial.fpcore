; -*- mode: scheme -*-

(FPCore (x)
 :name "Cancel like terms"
 (- (+ 1 x) x))

(FPCore (x)
 :name "Expanding a square"
 (- (* (+ x 1) (+ x 1)) 1))

(FPCore (x y z)
 :name "Commute and associate"
 (- (+ (+ x y) z) (+ x (+ y z))))
